name: Release

# Tag-triggered release: push `v<X.Y.Z>` and this workflow runs tests, sanity-
# checks that plugin.json/pyproject.toml agree with the tag, builds the dist
# bundle, and publishes a GitHub Release whose body is the CHANGELOG section
# for that version.
#
# We deliberately do NOT auto-release on merge to main — CHANGELOG entries
# and version bumps are editorial decisions made in PRs (see scripts/bump.py).
# The tag push is the "I really mean it" signal.
#
# The agora-dispatch step at the bottom is folded in from the old
# notify-agora.yml. Workflow chaining is blocked when the triggering event
# was caused by the default GITHUB_TOKEN (a release created by
# `gh release create` does NOT fire `release: published` downstream), so we
# dispatch inline with the AGORA_DISPATCH_TOKEN PAT.

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: write   # needed to create the GitHub Release

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-mock

      - name: Run tests
        env:
          PYTHONPATH: .
          PYTEST_ADDOPTS: "-p no:libtmux"
        run: pytest tests/ -q

      - name: Extract version from tag
        id: ver
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Verify manifest versions match the tag
        env:
          # Route step outputs and event fields through env vars rather than
          # inline-interpolating ${{ ... }} into the run script. Inline
          # interpolation happens before bash sees the script, which would
          # let a malicious tag name break out of the surrounding quotes.
          # While tag-pushing requires repo write (already a high bar),
          # env-var passing is defense-in-depth.
          TAG: ${{ steps.ver.outputs.version }}
        run: |
          python - <<'PY'
          import json, os, re, sys
          tag = os.environ["TAG"]
          pj = json.load(open(".claude-plugin/plugin.json"))["version"]
          py = re.search(r'^version\s*=\s*"([^"]+)"', open("pyproject.toml").read(), re.M)
          py = py.group(1) if py else None
          mismatches = []
          if pj != tag: mismatches.append(f"plugin.json={pj}")
          if py is not None and py != tag: mismatches.append(f"pyproject.toml={py}")
          if mismatches:
              print(f"::error::tag v{tag} does not match: " + ", ".join(mismatches))
              sys.exit(1)
          print(f"all manifests agree: v{tag}")
          PY

      - name: Build dist bundle
        env:
          TAG: ${{ steps.ver.outputs.version }}
          PYTHONPATH: .
        run: python -m scripts.release "$TAG"

      - name: Zip the dist bundle for release attachment
        env:
          TAG: ${{ steps.ver.outputs.version }}
        run: |
          cd dist
          zip -r "atelier-v${TAG}.zip" "v${TAG}"

      - name: Extract CHANGELOG section for this version
        id: changelog
        env:
          TAG: ${{ steps.ver.outputs.version }}
        run: |
          python - <<'PY' > /tmp/release-body.md
          import os, re, sys
          version = os.environ["TAG"]
          content = open("CHANGELOG.md", encoding="utf-8").read()
          # Match `## v<version> — date` block up to the next `## v` or end
          pattern = rf"^## v{re.escape(version)}\b.*?(?=^## v|\Z)"
          m = re.search(pattern, content, re.M | re.S)
          if not m:
              print(f"WARNING: no CHANGELOG section for v{version}", file=sys.stderr)
              print(f"## v{version}\n\nSee commit log for changes.")
          else:
              body = m.group(0).rstrip()
              # Strip the trailing `---` divider if present
              body = re.sub(r"\n---\s*$", "", body)
              print(body)
          PY
          echo "body_path=/tmp/release-body.md" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ steps.ver.outputs.version }}
          BODY_PATH: ${{ steps.changelog.outputs.body_path }}
        run: |
          gh release create "v${TAG}" \
            "dist/atelier-v${TAG}.zip" \
            --title "v${TAG}" \
            --notes-file "${BODY_PATH}"

      - name: Notify agora marketplace
        # Folded in from the old notify-agora.yml. We do it here instead of in
        # a separate workflow because workflow chaining is blocked when the
        # triggering event was caused by the default GITHUB_TOKEN: a release
        # created by `gh release create` above does NOT fire `release:
        # published` for downstream workflows. By dispatching inline, we don't
        # depend on that chaining — we just send the event ourselves with a
        # PAT (AGORA_DISPATCH_TOKEN), which agora's plugin-update.yml receives.
        env:
          GH_TOKEN: ${{ secrets.AGORA_DISPATCH_TOKEN }}
          TAG: ${{ steps.ver.outputs.version }}
        run: |
          if [ -z "$GH_TOKEN" ]; then
            echo "::error::AGORA_DISPATCH_TOKEN secret not set. Cannot dispatch to nitekeeper/agora."
            exit 1
          fi
          tag="v${TAG}"
          echo "Dispatching plugin-released to nitekeeper/agora (plugin=atelier, tag=$tag)"
          gh api repos/nitekeeper/agora/dispatches \
            -f event_type=plugin-released \
            -f client_payload[plugin]="atelier" \
            -f client_payload[tag]="$tag"
